[project]
name = "mcp-servers"
version = "0.1.0"
description = "Internal MCP server stubs: repo, family, site-readonly, ops-guarded"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
